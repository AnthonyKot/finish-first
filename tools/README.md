# Tools

Parsing and validation scripts will live here once the pilot PDF is selected. Prefer
small stages—fingerprint, extract, structure, verify, derive—so extraction errors do not
silently become editorial claims.
